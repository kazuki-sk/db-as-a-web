# config/prod.conf
listen_addresses = '*'

# メモリ設定
shared_buffers = 256MB  # コンテナのメモリ制限に合わせて調整
work_mem = 16MB         # BYTEA操作のために少し余裕を持たせる

# 接続設定
max_connections = 100   # PostgREST側のプールサイズと相談

# WAL設定 (読み取り専用ワークロードが多いため、デフォルトでも可だが以下を推奨)
wal_level = minimal
max_wal_senders = 0
